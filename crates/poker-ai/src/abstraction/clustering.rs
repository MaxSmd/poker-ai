//! K-Means++ clustering with L2 distance on EHS histograms.

/// Run K-Means++ clustering on `data` (rows = samples, cols = features).
/// Returns a cluster assignment for each sample.
pub fn kmeans_plus_plus(_data: &[Vec<f64>], _k: usize) -> Vec<usize> {
    todo!()
}
